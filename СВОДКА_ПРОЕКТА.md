# Проект «Путь наблюдателя» — сводка для Claude Project

## Что это
Сайт-курс + VK-бот с психологическими упражнениями + CRM для психолога.
Домен: putnabludatel.ru (HTTPS, Let's Encrypt)

## Инфраструктура
- **Сервер**: Timeweb VPS, IP 5.42.103.203, Ubuntu, PostgreSQL
- **Рабочая папка на сервере**: `/root/Observatory` (НЕ `/var/www/observatory` — это старая заброшенная копия, не используется)
- **База данных**: PostgreSQL, имя базы `put_nablyudatelya_db`, пользователь `observatory_user`
- **Веб-сервер**: nginx → gunicorn → Django
- **systemd-сервисы**:
  - `observatory` — основной сайт (Django+gunicorn)
  - `observer_bot` — VK-бот (python vk_bot/main.py)
  - Команды: `systemctl restart observatory`, `systemctl status observer_bot`, `journalctl -u observatory -n 30 --no-pager`
- **Автодеплой**: GitHub Actions, при `git push` в `main` — автоматически `git pull` на сервере + рестарт `observatory` (НЕ рестартит `observer_bot` автоматически — это нужно делать вручную)
- **Репозиторий**: github.com/Aaantooon/Observatory

## Структура Django-проекта
- `config/` — настройки, главный urls.py, settings.py, кастомная admin-панель (config/admin.py, с jazzmin темой)
- `myapp/` — курс, ментальная карта (React Flow), профиль, VK ID авторизация (myapp/vk_id_auth.py)
- `bot_api/` — модели и API для бота: User, Exercise, Result, ExerciseProgress, Notification, Review, Post
- `crm/` — CRM для психолога (проверка упражнений, карточки клиентов, контент-план)
- `vk_bot/` — отдельный процесс VK-бота (handlers.py, exercises/*, notifications.py, api_client.py)
- `templates/` — HTML. ВАЖНО: `home.html` НЕ наследует base.html, дублирует разметку сама

## Авторизация
- Вход через VK ID (новый протокол, PKCE, НЕ старый oauth.vk.com/social_django)
- Реализовано в `myapp/vk_id_auth.py`, роуты `/vk/login/`, `/vk/callback/`
- VK-приложение «Путь Наблюдателя» ID 54728530 в id.vk.ru/about/business/go (НЕ старый dev.vk.ru)
- Redirect URI = домен без пути (https://putnabludatel.ru/), обрабатывается прямо в `config.views.home`

## VK-бот — упражнения
1. Поиск стресса (stress_search.py) — есть, работает. 01.09.2026 доработан: текст
   подсказки к вопросу 1 (про общество/подчинение), и досрочная отправка на
   проверку наблюдателю ДО набора 100/полного разбора — двумя независимыми
   путями: (а) после 10 записанных образов в части 1 (кнопка «✅ Завершить
   и отправить», без разбора), (б) после разбора 3 образов в части 2 (та же
   кнопка, дальше можно не разбирать). Пороги — константы
   MIN_ITEMS_TO_FINISH_EARLY / MIN_ANALYZED_TO_FINISH_EARLY в начале файла.
   02.09.2026 (вечер) — атмосферный редизайн под тему «туман/фонарик/дорога»
   (см. Журнал изменений): новые тексты интро и меню упражнений, счётчик
   «отпущено/пугает» по всей карте, честная индикация условия досрочного
   завершения (не молчит, сколько ещё нужно), убран показ ложных 0% там,
   где разбора не было, экран подтверждения вопроса 1 упрощён до одной
   кнопки «Продолжить» с показом ВСЕХ написанных вариантов, в вопросе 3
   подставляется реальный введённый процент, добавлена защита от дублей
   пунктов (при записи и при вставке списком), на экране Вопроса 1/4
   добавлена кнопка «✏️ Изменить пункт» — можно поправить текст/оценку
   образа прямо там же, правка сохраняется в карте, в итоговой сводке и
   в result_data (а значит и в статистике на сайте)
2. Список счастья (happiness_list.py) — есть, работает
3. Мои роли (my_roles.py) — есть, работает. 02.09.2026: глубокая проверка,
   исправлены найденные баги (см. Журнал изменений)
4. Осознанный выбор (conscious_choice.py) — есть, работает. 02.09.2026:
   редизайн — разбор идёт по каждому записанному пункту, а не только по
   последнему; тем же вечером — уборка клавиатуры Шага 1 (убраны Назад/В
   начало/В конец), поддержка вставки списком (как в других упражнениях),
   Шаг 2 разбит на два экрана (сначала пользователь сам формулирует свою
   фразу-утверждение по нейтральному примеру, который специально не
   совпадает с его пунктом — иначе можно было просто скопировать, — потом
   уже вопрос «Кто отнял»), переписан текст Шага 3 (см. Журнал изменений)
5. Дневник (diary.py) — есть, работает. 02.09.2026: глубокая проверка (см.
   Журнал изменений)
6. Стоп-техника (stop_technique.py) — есть, работает. 02.09.2026: глубокая
   проверка (см. Журнал изменений)

Все наследуются от `vk_bot/exercises/base.py` (BaseExercise), используют self.save_result, self.save_progress и т.д.

## Система проверки упражнений (готово)
- Клиент отправляет упражнение → Review (status: pending/in_review/closed)
- Психолог комментирует в CRM (`/crm/review/<id>/`)
- Клиент получает комментарий в боте, может ответить
- Диалог хранится в Review.comments (JSON-список)
- Фоновая проверка новых комментариев — в vk_bot/notifications.py, цикл раз в 60 сек

## CRM (готово, /crm/)
- `/crm/` — список активных проверок
- `/crm/review/<id>/` — диалог по конкретной проверке
- `/crm/clients/` — все клиенты
- `/crm/clients/<id>/` — карточка клиента (упражнения, проверки, прогресс курса)
- `/crm/posts/` — контент-план постов (VK/Telegram/сайт, статусы)
- Доступ: @staff_member_required (нужен is_staff=True у пользователя)

## Django-сайт: форум, достижения, тест-результаты, черновики ботов (новое, 02.09.2026 ночь)
Крупный автономный блок правок сайта (не тронул vk_bot/, все 253 теста бота
остались зелёными без единой правки в тестах). Ничего из этого НЕ задеплоено
автоматически — файлы переданы пользователю, дальше обычный git push +
git pull + перезапуск (см. ниже, для этого блока — с миграциями).
- **Форум (django-machina)**: был полностью настроен в settings.py, но роут
  был закомментирован. Раскомментирован `path('forum/', include('machina.urls'))`
  в `config/urls.py`, в шапку сайта (`templates/base.html`) добавлена ссылка
  «Форум». django-machina сам ничего не создаёт при первом запуске — после
  миграций форум технически доступен по `/forum/`, но пуст: нужно один раз
  зайти в админку, создать хотя бы один раздел форума (Forum) и выдать
  группе аутентифицированных пользователей права на чтение/ответ
  (Permissions → machina), иначе обычные пользователи увидят пустой форум
  без прав писать.
- **Достижения и геймификация** ("стать Наблюдателем" — было в «Не начато»):
  новые модели `Achievement`/`UserAchievement` (`myapp/models.py`), список
  условий и их проверка — `myapp/achievements.py` (`ACHIEVEMENTS`,
  `check_and_award(user)`, вызывается при завершении модуля, добавлении
  комментария, сохранении ассоциации в игре и прохождении теста — везде,
  где могло появиться новое условие). 10 достижений на старте (первый
  модуль, середина курса, курс пройден, серии 3/7/30 дней, первая/10-я
  ассоциация в игре, 100% за тест, первый комментарий). Страница
  `/achievements/` (`achievements_view`), ссылка в шапке сайта, узел на
  ментальной карте («🏆 Достижения (N)», отдельная ветка от корня).
  Прежде чем показывать пользователям — накатить миграции (новые таблицы).
- **Результаты теста модуля** (`Module.test_questions`) раньше проверялись
  только в браузере (JS) и нигде не сохранялись — не было видно ни в CRM,
  ни в статистике. Новая модель `ModuleTestResult` (лучший результат,
  число попыток), эндпоинт `POST /api/course/test-result/`
  (`submit_test_result_api`), `templates/myapp/course/module.html`
  отправляет результат теста после подсчёта на клиенте и показывает
  баннер «🏆 Новое достижение», если тест дал 100% (достижение «Отличник»).
- **Черновик мультиплатформенных ботов** (`platform_bots/`, новый пакет,
  НЕ подключён никуда, ни один systemd-сервис его не запускает) —
  архитектура на будущее под Telegram/MAX/Одноклассники (и по тому же
  образцу — любую другую). `base_adapter.py` — общий интерфейс
  `MessagingAdapter` (send_message/run) и нейтральный формат клавиатуры
  (просто список рядов кнопок-строк, без привязки к VK). `telegram_adapter.py`
  — рабочий черновик поверх официального Telegram Bot API, можно проверить
  прямо сейчас через `@BotFather`-токен (см. `if __name__ == "__main__"` в
  файле). `max_adapter.py`/`ok_adapter.py` — скелеты с реалистичной
  структурой запросов, но БЕЗ подтверждённых точных названий методов/
  параметров бот-API этих платформ — везде явно помечено `TODO: сверить
  с докой`, использовать только после сверки с официальной документацией
  каждой платформы. `platform_bots/README.md` — почему не подключено сразу
  в vk_bot/ (там вся логика упражнений завязана на VK-формат клавиатур
  напрямую) и пошаговый план миграции, когда решите подключать по-настоящему,
  плюс отдельное предупреждение про ловушку с ID пользователя (он разный на
  каждой платформе, `bot_api.User` сейчас хранит только `vk_id`).

**Деплой этого блока (в этот раз ЕСТЬ новые модели — обычной последовательности
git pull + restart недостаточно, нужны миграции):**
```
cd /root/Observatory
git pull
source venv/bin/activate   # или venv/bin/python ниже, если venv не активируется
python manage.py check                     # ловит опечатки/несовместимости до миграций
python manage.py makemigrations --dry-run  # посмотреть, что создастся
python manage.py makemigrations myapp
python manage.py migrate
python manage.py collectstatic --noinput   # нужно для статики форума (django-machina)
sudo systemctl restart observatory
```
`observer_bot` этот блок вообще не касается — его перезапускать не нужно.
После миграций — один раз зайти в админку и настроить права форума (см. выше).

## Известные грабли / паттерны ошибок в этом проекте
1. **Расхождение реальной структуры БД и Django-моделей** — таблицы создавались под старые версии моделей, часто не хватает колонок с дефолтами (is_active, created_at, updated_at) — чинится через `ALTER TABLE ... SET DEFAULT`
2. **`.env` содержит опечатки** — лишние пробелы/кавычки после `=`, разные имена переменных для одного смысла (VK_TOKEN vs VK_GROUP_TOKEN)
3. **DRF router bugs** — методы `@action` иногда вставляются не в тот класс ViewSet (проверять `grep -n "^class "` при ошибках)
4. **URL-адреса в api_client.py не совпадают с bot_api/urls.py** — регулярно нужно сверять
5. **Дублирование кода при правках через nano на сервере** — сервер и git расходятся, чинить через `git checkout -- <file>` перед `git pull`
6. **`/var/www/observatory` — НЕ ТРОГАТЬ**, это старая копия, не связанная с продакшеном
7. **Слияние двух записей с уникальными полями (`vk_id`/`telegram_id` и т.п.)** — удалять запись-источник этих значений НАДО ДО того, как сохранять их копию на другой записи, иначе `IntegrityError: UNIQUE constraint failed` (значение ещё занято старой строкой). См. `bot_api/views.py::_merge_users`, журнал изменений 03.09.2026 «Привязка аккаунтов».

## Как обычно чиню баг (алгоритм)
1. `journalctl -u observatory -n 30 --no-pager` или `journalctl -u observer_bot -n 30 --no-pager`
2. Найти traceback, определить файл и строку
3. Править ЛОКАЛЬНО в VS Code (не на сервере!)
4. `git add . && git commit -m "..." && git push`
5. На сервере: `cd /root/Observatory && git pull && systemctl restart observatory` (и/или `observer_bot`)
6. Если ошибка БД — проверить `python manage.py showmigrations bot_api`, при расхождении — правки через `sudo -u postgres psql -d put_nablyudatelya_db`

## Не начато / в планах
- Telegram-бот (`vk_bot/main_telegram.py`) полностью на связи: упражнения,
  фоновые напоминания и проактивная доставка комментариев психолога — всё
  работает (см. журнал изменений, 03.09.2026, шаги 4 и 5). Привязка одного
  человека к нескольким платформам через код подтверждения — сделана (см.
  журнал изменений, «Привязка аккаунтов»). Не сделано: ротация
  Telegram-токена/старых `.env`-секретов (не срочно, отложено по решению
  пользователя). MAX и Одноклассники — только сверенные с докой черновики
  адаптеров в `platform_bots/`, ни один не подключён к живому боту.

## Журнал изменений
Коротко и по делу, свежее сверху — обновляется после каждой заметной правки.

### 2026-09-05 — возврат смягчённого: +13 карточек, 482 из 469

Правило Антона: «ничего не вырезать и не переделывать — как автор видит, так и делай
карточки». Прошёлся по местам, где при первой сборке я смягчал, обходил или домысливал.

Снял оговорки и вернул авторские формулировки:
- **266** «Мышление "не хватает"» — убрал приписку «если трактовка кажется натянутой,
  перепишу мягче». Формулировка про наркоманию и алкоголизм оставлена как у автора.
- **268** «Они очищали нацию» — развернул довод «им везло» целиком, вместе с тем, как
  готовили молодёжь: лягушки → «это животные» → «это расы, не люди». Флаг «сомневаюсь
  в трактовке» убран.
- **93** «Тест на ядовитость» — вместо моей реконструкции стоят авторские цифры:
  материальный ущерб 1-0, поведение 6-0, вера — семь миллиардов минус один,
  ситуация — знак бесконечности. Пометка про «неразборчиво» снята.
- **30** «Волки и овцы» — вернул шута («сегодня это оппозиция») и ссылку на продолжение.

Новые карточки, которых в первой сборке не было (470–482):
- 470 «Бета-самец учредитель» (тема 1) — Сталин, убранная оппозиция, потеря обратной связи.
- 471 «Семьдесят процентов мусора» (тема 2) — беременность, недопережитые переживания.
- 472 «Им везло» (тема 9) — одно действие, две карты.
- 473–478 (тема 11) — чакры «оголи/прикрой» и провайдер при крещении; «самый тупой мужик
  ближе к Богу»; секс как взаимодействие полов, начинающееся сверху; скандал как заменитель
  секса; проклятия только женские; третья проверка интуиции — «кого бы я обняла ногами».
- 479 «Сосуд немощный» (тема 12) — приоритетность и «скромная задача стать Богом».
- 480–482 (тема 13) — вечер «кому за 30»; немецкие эксперименты про отогревание;
  деление яйцеклетки без мужчины.

Правка шаблона: `.kcard .quote .src` был `white-space: nowrap` — длинное поле `also`
не переносилось и вылезало за карточку. Добавлен класс `.note` с нормальным переносом.

Итого: **482 карточки, 558 связей, 15 тем из 15**, линтер чист (цитаты сверены построчно,
геометрия SVG без вылетов и наложений). Линза: гнев 80, вина 115, в обоих корнях 7.

- **05.09.2026 (Карточки курса — витки на `/cards/`, линза корня, курс закрыт)** —
  все 15 тем разобраны, пост-обработка из `ЗАДАЧА_КАРТОЧКИ.md` сделана
  целиком. В `myapp/data/cards.json` — 469 карточек и 538 связей.
  - **Настоящие межлекционные связи пересчитаны**: 318 переходов внутри
    тем и 220 между темами (в прототипе на фейковых узлах их было 18 для
    примера). Связи не выдумывались задним числом — они ставились по
    ходу разбора, там, где автор сам возвращается к прежней формуле;
    здесь их только пересчитали и добавили один недостающий переход
    («ищем там, где светло» → «первый признак — интерес»), чтобы не
    осталось ни одной карточки без переходов. Самые плотные пары:
    11↔12 (14), 11↔14 (9), 03↔07 и 01↔13 (по 7).
  - **Линза «корень: гнев / корень: вина» собрана на реальном
    материале**: у карточек появилось поле `roots` (`gnev` / `vina`,
    может быть и то и другое). Гнев объясняет 76 карточек, вина — 112,
    в обоих корнях 7. Классификация ручная, по содержанию карточки:
    в гнев — разрушение, борьба, месть, обида-как-атака; в вину — долг,
    «должен», табу, стыд, роль-обязанность, жертва. Видно ровно то, о
    чём говорил автор: тема 09 (Либидо-Мортидо) почти целиком гнев
    (21 из 36), тема 15 и тема 03 — почти целиком вина.
  - **Механика «Витки» перенесена с прототипа на `/cards/`**: вместо
    плоского списка — круг из 15 колец (кольцо = тема, размер = число
    карточек, толщина линии между кольцами = число переходов). Тап по
    кольцу раскрывает виток: тема уезжает в центр, её карточки встают
    венцом вокруг, остальные темы уходят на внешнюю дугу — из зума
    по-прежнему виден весь круг. Линза подсвечивает кольца и показывает
    на каждом «сколько из скольких».
  - **Открытая тема отдаётся сервером, а не прячется CSS**: 469 карточек
    со схемами — это больше мегабайта разметки, телефон такую страницу
    открывает заметно медленнее. Номер темы приходит в `?l=`, корень —
    в `?root=`, поэтому обзор весит 25 КБ, страница темы — 60–130 КБ,
    ссылка на конкретный виток остаётся рабочей, и всё работает без JS.
    JS сверху только подменяет панель темы без перезагрузки.
  - **Удалён `/api/mindmap/rings/`** (`mindmap_rings` в `myapp/views.py`,
    роут в `myapp/urls.py`) — задел под кольца на модулях курса; механика
    переехала в `/cards/` и работает на карточках, а не на модулях. На
    фронте эндпоинт нигде не вызывался, так что удаление ничего не рвёт.

- **05.09.2026 (Карточки курса — темы 04–14, +296 карточек)** —
  разобраны все оставшиеся лекции, по одной за партию, в том же формате:
  тема 04 «Нереализованные желания» (32 карточки, id 97–128), 05 «Роли и
  маски I» (34, id 129–162), 06 «Роли и маски II» (30, id 163–192),
  07 «О любви» (28, id 193–220), 08 «Три этапа прощения» (25, id 221–245),
  09 «Либидо — Мортидо» (36, id 246–281), 10 «Три пути прохождения кармы»
  (36, id 282–317), 11 «Мужчины и женщины I» (38, id 318–355),
  12 «Мужчины и женщины II» (38, id 356–393), 13 «Слово есть ложь»
  (38, id 394–431), 14 «Типология» (38, id 432–469).
  - Каждая цитата проверена grep'ом по расшифровке; в нескольких местах
    номер строки сдвигался на строку-две, чтобы цитата целиком попадала
    в окно проверки.
  - Спорное не додумывалось: карточка 268 («они очищали нацию», тема 09)
    помечена в `also` как самый скользкий пример темы — довод автора про
    «им везло» намеренно не развёрнут, вопрос вынесен Антону.
  - Схемы проверялись не глазами, а линтером `lint.py` (геометрия SVG:
    вылет за viewBox, наложения подписей + сверка цитаты со строкой
    расшифровки). Финальный прогон по всем 469 карточкам — чисто.

- **05.09.2026 (Карточки курса — тема 03 «О важном: 3 основных табу», 35 карточек)** —
  четвёртая партия: разобрана третья лекция (строки 1685–4346, самая
  длинная из пройденных) — 35 карточек, id 62–96. В
  `myapp/data/cards.json` теперь 96 карточек и 114 связей (темы 01, 02,
  03 и 15). С темы 03 расшифровка идёт сплошной прозой, поэтому все
  цитаты взяты прямо из этой лекции.
  - Линия темы 03: важно и пустяк (вся конфликтология) → упражнение
    «каша, муж и таракан» → направление есть, маршрута нет → мудрость =
    ум × опыт → боль и страх как инструменты власти → вина как боль души
    → «хороший = делает, что положено» → вина включается на желании →
    табу («не делать нельзя, а даже хотеть нельзя») → три табу:
    инакомыслие, тело, пол (плюс третье мужское) → запрет вместо
    информации → «чем сильнее запрет, тем сильнее изврат» → табу на
    счастье → выход: объяснить опасность, а не запретить («кипяток»).
  - Второй блок: жизнь-лечебница, «смерти никто не боится», что общего у
    всех религий, выбор как отсутствие программы, ответственность против
    долга, лента Мёбиуса, «мы выбираем только эмоциональное
    реагирование», клетка и организм.
  - Третий блок (практика): три уровня ценностей → зацепка меряется
    врагами (на уровне идей их 7 миллиардов минус один) → бабушки у
    подъезда → гордыня → тест на ядовитость → второй список «как должно
    быть» с добавкой «в моём присутствии» → комары → «вселенная
    многопупочна».
  - Межлекционных связей стало заметно больше: «чем сильнее запрет»
    (03) ↔ «должен = скрытый запрет» (15); лента Мёбиуса (03) ↔
    «Хочу/Должен/Выбираю» (15); вина как боль души (03) ↔ «Вина 50/100»
    (15); «хороший = делает, что положено» (03) ↔ «Хороший/Плохой» (15);
    жизнь-лечебница (03) ↔ жизнь-школа (01) и «мешок» (02); клетка и
    организм (03) ↔ депрессия (02); списки из тем 01, 02 и 03 связаны в
    одну линию. Карточкам 8 и 9 темы 15 проставлено `also` — автор
    повторяет тест на пальцах и формулу «на чужую волю энергии не
    даётся» в теме 03 (стр. 3200 и 3234).
  - Осторожно с одним местом: в карточке 93 («тест на ядовитость»)
    точные множители в расшифровке записаны неразборчиво («1-0», «6-0»),
    поэтому уровни взяты по смыслу из авторского же счёта врагов
    (стр. 3627–3684), о чём сказано в поле `also`.
  - Все 35 цитат сверены с исходником построчно; схемы прогнаны рендером
    в браузере, поправлены вылезавшие подписи в 12 карточках.

- **05.09.2026 (Карточки курса — тема 02 «Психосоматика», 27 карточек)** —
  третья партия: разобрана вторая лекция (строки 815–1684) — 27 карточек,
  id 35–61. В `myapp/data/cards.json` теперь 61 карточка и 70 связей
  (темы 01, 02 и 15).
  - Линия темы 02: без стрессов жить нельзя → зачем организм даёт стресс
    («не слышим — включает погромче») → схема «соль и сахар 1–10», где
    запрет внутреннего родителя всегда на шаг выше желания → накопленное
    выливаем дома («когда налито с горочкой, стол лучше не трогать») →
    двойной тонус (один раз чтобы сделать, второй чтобы сдержаться) →
    зажимы, спина, соли, граната с выдернутой чекой.
  - Второй блок — компенсаторика: почему манекен у нас не бьётся,
    заменитель победы и рост дозы, велосипед («ничего вкуснее победы
    нет»), зависимость, депрессия как защитная реакция, ребёнок за
    уроками (невроз ≠ депрессия), «из воронки выходят только вглубь».
  - Третий блок — карта тела: правая/левая сторона, вина сверху вниз по
    убыванию осознанности, зажимы по зонам, органы и чувства, доска на
    десятом этаже (страх забирает до 80 % ресурса), «владеешь информацией
    — владеешь ситуацией» и возврат к списку с десятибалльной шкалой.
  - Ядро (`core`) темы 02: «не слышим — включает погромче», соль и сахар,
    двойной тонус, граната, компенсаторика, депрессия, «из воронки
    вглубь», карта тела, доска на десятом этаже, «владеешь информацией».
  - Найдены и проставлены настоящие межлекционные связи: «владеешь
    информацией» (02) ↔ «пока не понимаем — управляет» (01) — это
    буквально одна формула; список 100 пунктов и «продавщица» (01) ↔
    «не стрелять по мухам из пушки» (02); соль и сахар (02) ↔ «вина
    включается на желании» (15); воронка депрессии (02) ↔ воронка вины (15).
  - Не взято в карточки сознательно: блок про беременность и «70 %
    недопережитых переживаний» (стр. 1254–1274) — трактовка из конспекта
    восстанавливается неоднозначно, вынесен на решение автора.
  - Все 27 цитат сверены с исходником построчно; схемы прогнаны рендером
    в браузере и поправлены по вылезающим подписям.

- **05.09.2026 (Карточки курса — тема 01 «Карты реальности», 24 карточки)** —
  вторая партия по `ЗАДАЧА_КАРТОЧКИ.md`: разобрана первая лекция курса
  (строки 2–814 расшифровки) — 24 карточки, id 11–34, у всех проставлено
  новое поле `lecture`; старым десяти карточкам темы 15 задним числом
  проставлено `lecture: 15`, чтобы формат был единым. Всего в
  `myapp/data/cards.json` 34 карточки и 38 связей.
  - Состав темы 01: вводные правила («не верить, а проверить»; «пока не
    понимаем — явление управляет нами»; «врите только себе»), механика
    восприятия (два полушария, слов меньше, чем явлений, стресс как
    несовпадение прогноза с реальностью, 28,5 лет за секунду, принцип
    реальности), рабочий блок (менять не ситуацию, а оценку; список на
    100 пунктов; разбор «продавщица в носу») и сами карты реальности:
    райский сад/джунгли → соревнование → война → игра → карьера
    (принцип курятника) → волки и овцы (примитивная группа, Сталин без
    обратной связи) → мораль/нравственность → жизнь-школа → «не знать,
    как правильно».
  - Ядро (`core`) темы 01: понял — значит можешь изменить; стресс как
    несовпадение; 28,5 лет; менять оценку, а не ситуацию; карта реальности;
    «не знать, как правильно».
  - **Важная особенность источника**: темы 01 и 02 записаны в расшифровке
    не сплошным текстом, а деревом-конспектом (xmind-выгрузка) и с
    опечатками, темы 03–15 — дословной прозой. Поэтому там, где формула
    первой лекции звучит дословно в более поздней теме, цитата взята
    оттуда, а в поле `also` указано, где она в теме 01 (так сделано для
    стресса, 28,5 лет, морали/нравственности и «все наши беды оттого, что
    мы якобы знаем, как правильно»). Все 24 цитаты сверены с исходником
    построчно скриптом — совпадение точное.
  - Схемы проверены рендером в браузере (headless Chromium, скриншоты),
    поправлены вылезавшие за 460px подписи в карточках 17, 22, 23, 25, 29
    и форма полушарий в 14.
  - Правится только файл `myapp/data/cards.json`, миграций и перезапуска
    бота не требуется — деплой сайта как обычно (`git pull` + перезапуск
    gunicorn/uwsgi).

- **05.09.2026 (Карточки курса — починен меловой шрифт и цвета схем)** —
  на `/cards/` схемы-SVG рисуются CSS-переменными (`--font-chalk`,
  `--accent`, `--warn`, `--cool`, `--t-image`, `--ink*`, `--line`), которые
  раньше были определены только в черновике-прототипе, а в
  `templates/myapp/cards.html` отсутствовали — схемы падали в серый
  безымянный шрифт вместо рукописного и без цвета. Добавлены все переменные
  в `.cards-page`, подобраны под тёмную тему сайта; `.schema svg` сужен до
  560px по центру. Деплой подтверждён (324 теста зелёные).
  - Решено: план дальше — план по наполнению курса карточками и переносу
    механики «Витки» (кольца-лекции, выбрана после сравнения ~10 вариантов
    в прототипе «Карта наблюдателя») на сайт зафиксирован отдельным файлом
    `ЗАДАЧА_КАРТОЧКИ.md` в корне репозитория — там же критерии качества
    (передать логику и образы автора, не пересказ) и формат карточки.
    Заготовка `/api/mindmap/rings/` будет удалена, когда витки переедут
    на `/cards/` — дублировать одну и ту же идею двумя эндпоинтами не нужно.

- **03.09.2026 (Карточки курса — материал лекций как карточки-формулы)** —
  начали превращать расшифровку лекций (39 419 строк) в карточки: одна
  карточка = один смысл, у неё тип (формула / понятие / образ / история /
  практика), схема «как на доске», дословная цитата автора с номером строки
  и связи с другими карточками. Появилась страница `/cards/`
  (`myapp/views.py::cards_view`, шаблон `templates/myapp/cards.html`), блок
  «Курс-ассоциации» на `/flashlight/` теперь ведёт на неё (кнопка «К модулям
  курса» осталась второй ссылкой).
  - **Без модели и без миграции**: карточки — авторский материал, а не данные
    пользователя, поэтому лежат файлом `myapp/data/cards.json` и правятся
    правкой файла + `git pull`. Уровень освоения («услышал / заметил в жизни /
    применил» — три пути прохождения кармы по автору) хранится в localStorage
    у самого человека.
  - Первая партия — 10 карточек маршрута «Должен → Выбираю» (тема 15):
    опасно/полезно, хороший/плохой, вина 50 % и 100 %, свобода воли,
    должен = скрытый запрет, огонь под ретортой, воронка вины, тест на
    пальцах, хочу/должен/выбираю, «я имею право не хотеть». Все цитаты
    сверены построчно с исходником.
  - **Задел под карту-витки**: добавлен эндпоинт `/api/mindmap/rings/`
    (`mindmap_rings`) — модуль курса отдаётся как «кольцо», внутри его
    ключевые понятия и ассоциации, а связи между кольцами считаются по
    общим словам (если понятие встречается в двух модулях — автор к нему
    возвращается). Старая карта на React Flow пока работает как работала.
  - Деплой без миграций: `git pull` + `systemctl restart observer_bot`
    (для сайта — перезапуск gunicorn/uwsgi, если он отдельный).

- **03.09.2026 (Осознанный выбор — финальный экран показывает разбор, а не только счётчик)** —
  пользователь на экране «ПУТЬ ЗАВЕРШЁН» написал «Мне кажется не полный
  ответ»: финал показывал только «Разобрано пунктов: N» без единого слова о
  том, что реально было разобрано (в отличие от «Дневника», где финал эхом
  повторяет все ответы). Добавлены `_truncate_for_display` (как в diary.py)
  и `_format_analysis_summary` (`vk_bot/exercises/conscious_choice.py`) — в
  `_finish` теперь встроена короткая сводка по каждому разобранному пункту
  (📌 сам пункт + обрезанные до 150 симв. «минусы/плюсы выбора» и
  «альтернатива»), максимум 5 пунктов, если разобрано больше — строка
  «…и ещё N пункт(ов) разобрано». Тесты на точный текст финального
  сообщения не завязаны — полный пакет остался зелёным (324/324). Деплой —
  обычная правка кода без миграций, `git pull` + `systemctl restart
  observer_bot`.

- **03.09.2026 (Дневник — три блока в течение дня: Утро / День / Вечер, стоп-техника подключена к напоминаниям)** —
  по просьбе пользователя «Дневник» (`vk_bot/exercises/diary.py`) больше не
  один линейный разговор на 6 шагов подряд, а три захода в течение дня —
  ближе к тому, как реально живёт человек (сон вспоминают сразу после
  пробуждения, а не сидят потом час над остальными вопросами):
  - **Утро** — только «Сон» (шаг 1).
  - **День** — «Настроение», «Тело», «Мысли», «Чего хочу» (шаги 2-5), где-то
    примерно через час после утра.
  - **Вечер** — «Чем этот день отличается от других» (шаг 6).
  - Между блоками (`PHASE_BLOCK`, `_show_block_boundary`) сессия
    завершается — как при обычной отмене: прогресс уже сохранён, человек
    свободен заняться другими делами (например, стоп-техникой) и не должен
    ждать на месте. При возвращении в «Дневник» resume-подсказка теперь
    честно называет следующий блок («Дальше: ☀️ День» и т.п.).
  - **Не блокируем реальным часом** — как решил пользователь: «Продолжить»
    работает сразу же, даже до того как час прошёл, никакой проверки
    времени нет. На переходе Утро → День бот сам (без нажатия кнопки)
    ставит одноразовое push-напоминание через час
    (`self.api.create_notification(..., 'diary_day', 'once', {"delay_hours": 1})`
    — новый псевдо-тип `diary_day` в `notifications.py::_get_reminder_text`,
    текст другой, чем у обычного утреннего «Доброе утро»). На переходе
    День → Вечер отдельного авто-напоминания нет.
  - Вопрос «Тело» (шаг 3) переписан явно под напряжение/расслабление и
    головную боль (раньше — общий «Что чувствуешь?»), по решению пользователя
    остался ОДНИМ вопросом, не разбит на 2-3 отдельных шага.
  - Экран «Чего я хочу?» (шаг 5) теперь показывает короткую сводку
    настроения/тела/мыслей (`_format_day_recap`) прямо перед вопросом — по
    просьбе пользователя ответ должен опираться на то, что уже написано
    выше (например: болит спина → решить посидеть на работе), а не
    сочиняться с нуля.
  - **Стоп-техника «как можно чаще» в течение дня** — довели до конца уже
    существовавшую, но не подключённую функцию `setup_stop_technique_reminder`
    в `notifications.py` (текст меню «⏰ Напоминания» уже обещал этот пункт,
    кнопки и обработчика не было): добавлена кнопка «🛑 Стоп-техника в
    течение дня» в `keyboards.py::get_reminder_keyboard()` и обработчик в
    `handlers.py` (состояние `'reminders'`) — ставит 3 ежедневных
    напоминания (12:00/15:00/18:00, времена пока не настраиваются
    пользователем).
  - **Тесты**: `tests/test_exercises.py` — 4 новых теста на границы блоков
    (сессия закрывается, напоминание ставится только на Утро→День, resume
    называет следующий блок, «Продолжить» не ждёт часа) + переписаны все
    существующие diary-тесты, пересекающие границу блока (добавлен хелпер
    `_diary_resume`, имитирующий «заглянул попозже»). `tests/test_handlers.py`
    — 2 новых теста на кнопку стоп-техники в меню напоминаний + правка
    теста диспетчеризации follow-up-сообщений (dream теперь сразу закрывает
    сессию, проверяем текст интерстишла вместо содержимого сессии).
    `tests/conftest.py::FakeNotificationSystem` — добавлен
    `setup_stop_technique_reminder`. Полный пакет: 324/324.
  - Деплой — обычная правка кода без миграций, `git pull` + `systemctl
    restart observer_bot`.

- **03.09.2026 (Осознанный выбор — тематический текст: туман/фонарик/дорога, шаг 1 из «Варианта B»)** —
  по итогам разбора, чего не хватает в оформлении бота (несогласованная
  терминология между упражнениями, тема тумана/фонарика есть только в
  «Поиске стресса»), пользователь выбрал полный редизайн («Вариант B»), но
  решил делать его поэтапно, по одному упражнению, начиная с текста/терминов
  без структурных изменений (карта наблюдателя, дневник наблюдателя —
  отдельным будущим этапом) и пока не трогая сайт. Это первый шаг —
  `vk_bot/exercises/conscious_choice.py`:
  - Эмодзи заголовка 🧘 → 🔦 (везде: вступление, экран продолжения
    сохранённого прогресса, финиш).
  - Вступление шага 1 дополнено рамкой темы: «Туман "надо" прячет то, что на
    самом деле всегда было твоим выбором. Возьми фонарик — сейчас пройдём
    через него по одному пункту».
  - «Разбираем пункт N/M» → «🔦 Освещаем пункт N/M» (шаг 2, оба варианта
    экрана).
  - Финишное сообщение дополнено строкой «Каждый раз, доставая фонарик для
    пункта из списка "надо", ты находишь дорогу, которую выбрал сам».
  - Сообщение при отмене/выходе переписано в стиле «Поиска стресса»: «🌫️
    Путь подождёт в тумане / Прогресс сохранён / Фонарик ждёт тебя, чтобы
    продолжить».
  - Только текст, никакой логики/структуры/тестов не менялось — 318/318
    проходят как есть. Деплой — обычная правка кода без миграций.
  - Дальше (не начато): по одному упражнению добавлять ту же тематику
    (`stress_search.py` уже её использует и станет образцом словаря),
    сайт и структурные изменения (карта/дневник наблюдателя) — отдельно,
    когда дойдёт очередь.
  - Правка формулировки на экране шага 4 (`_show_choice_minus`): «Не хочу»
    → «Не хочу чтобы...», примеры-подсказки переписаны с «Дети будут
    голодными / Будут жаловаться / Будут ныть» на «Голодали / Жаловались /
    Ныть» (по просьбе пользователя, без изменения логики).
  - Симметричная правка на экране «Хочу» (`_show_choice_plus`): «Хочу
    (полезные плюсы):» → «Хочу» (убрана уточняющая пометка и двоеточие,
    примеры-подсказки не менялись).
  - На экране «Шаг 5: Альтернативы» (`_show_alt_minus`) добавлена вторая
    строка сразу под «Иногда выбираю не делать это «X»»: «Иногда выбираю
    «X»» — обе стороны выбора (делать/не делать) видны рядом. В
    `_show_alt_plus` пока не дублировано — если понадобится, добавить туда
    же.
  - **Прогресс-бар унифицирован по всему боту** (`exercises/base.py::_get_progress_bar`,
    общий для всех упражнений на `BaseExercise` — happiness_list, my_roles,
    conscious_choice, diary, stop_technique): нейтральный `▰▰▱▱ N%` заменён
    на тематический `🔦▮··· N%` (луч фонарика в тумане) — ровно та же
    формула, что уже была в `stress_search.py`. Один общий метод — правка
    в одном месте меняет прогресс-бар везде разом. 5 тестов в
    `tests/test_exercises.py` обновлены под новый символ (`▮` вместо `▰`).
  - **Кнопки «Назад / В начало / В конец» убраны из клавиатуры**
    `keyboards.py::step_nav_keyboard()` (общая для diary.py и
    stop_technique.py — правка в одном месте убрала лишний ряд кнопок сразу
    из обоих упражнений) — по просьбе пользователя, визуальный шум.
    Обработчики (`_handle_back`/`_handle_to_start`/`_handle_to_end`) не
    трогали — навигация всё ещё работает, если написать «назад»/«в
    начало»/«в конец» текстом (как уже было в conscious_choice, где этих
    кнопок никогда не было). `tests/test_exercises.py::STEP_NAV_KEYBOARD_BUTTONS`
    обновлён (теперь совпадает с обычным набором из 3 кнопок).
  - Все правки этого дня (тема + формулировки + прогресс-бар + кнопки) —
    318/318 тестов. Деплой — обычная правка кода без миграций,
    `git pull` + `systemctl restart observer_bot`.
  - Ещё одна правка формулировки на вступлении шага 1: «Туман "надо" прячет
    то, что...» → «Туман прячет долги, что...»; убрано упоминание «через
    «;»» в подсказке по формату ввода (сам разбор по «;» в
    `_split_items_text` не тронут, просто в тексте больше не упоминается) —
    324/324.
  - **Найден и исправлен обман**: экран «Не хочу чтобы...» (шаг 4,
    `_show_choice_minus`) обещал «Каждый раз разные примеры», но пример был
    один и тот же («Голодали/Жаловались/Ныть» — сценарий «Кормить детей»)
    при ЛЮБОМ «Я выбираю», даже если пункт был совсем не про детей (например
    «Помогать»). Теперь `_CHOICE_MINUS_EXAMPLE_SETS` — 5 наборов примеров, не
    привязанных к конкретному сценарию, `random.choice` на каждый показ
    экрана — надпись наконец говорит правду.
  - Правка формулировки на «Шаг 5: Альтернативы» (`_show_alt_minus`):
    «Иногда выбираю «X»» → «Иногда выбираю не «X»»; «❓ Не хочу\nКогда» →
    «❓ Не хочу или Не хочу когда...» (одной строкой вместо двух).

- **03.09.2026 (Осознанный выбор — полный сбор пунктов на каждом экране, показ разбираемого пункта)** —
  по серии правок пользователя (после мержа шага 4, см. запись ниже) все
  четыре экрана «Не хочу»/«Хочу» (`conscious_choice.py`) переведены на тот же
  принцип, что и сбор шага 1: писать можно сколько угодно раз, по одному
  пункту или сразу списком, и шаг НЕ продвигается сам по себе — только явное
  «Продолжить» переводит дальше (`_collect_items`, обобщённый обработчик).
  Раньше эти экраны принимали только один ответ и сразу переходили дальше,
  молча теряя предыдущий ввод при повторном сообщении.
  - Заодно объединены «Шаг 5: Альтернативы» и «Не хочу (другие минусы)» —
    тем же способом, что и шаг 4 (см. запись про объединение шага 4).
  - Правки формулировок: «Иногда «X» можно не делать» → «Иногда выбираю не
    делать это «X»»; «Каждый раз разные примеры» → «Когда» (для экрана
    альтернативных минусов); «Хочу (другие плюсы)» → «Каких плюсов хочу?».
  - На каждом из экранов разбора (не только на первом) теперь всегда видно,
    какой именно пункт сейчас разбирается — «📌 Я выбираю: «X»» или «Иногда
    выбираю не делать это «X»» показывается заново на каждом шаге.
  - Тесты `tests/test_exercises.py` переписаны под новое поведение
    (многострочный ввод, разбор каждого записанного пункта по очереди, а не
    только последнего, отсутствие автопродвижения по тексту) — 318/318 по
    всему пакету. Деплой — обычная правка кода без миграций.

- **03.09.2026 (Привязка аккаунтов — диплинк вместо ручного набора кода)** —
  по просьбе пользователя привязка VK+Telegram (см. запись выше) стала
  ещё удобнее: код теперь приходит кликабельной ссылкой, а не только
  текстом для перепечатывания.
  - **Из VK в Telegram**: `https://t.me/<TELEGRAM_BOT_USERNAME>?start=link<код>`
    — Telegram сам присылает боту `/start link<код>` при переходе по
    ссылке, подтверждение происходит СРАЗУ, без единого лишнего действия
    (даже без открытия меню — сработает и как самое первое сообщение от
    совершенно нового в Telegram человека). Показывается только если в
    `.env` заполнен `TELEGRAM_BOT_USERNAME` — это НЕ секрет (публичный
    @username бота), но по умолчанию пуст; без него функция просто не
    показывает ссылку, остальная привязка (текстом) не страдает.
  - **Из Telegram в VK**: `https://vk.me/write-<VK_GROUP_ID>?text=<код>` —
    у VK, в отличие от Telegram, нет диплинков с произвольным payload,
    зато можно предзаполнить текст сообщения — остаётся нажать
    «Отправить» в самом VK (одно нажатие вместо перепечатывания 6 цифр).
  - **Распознавание кода** (`handlers.py::_extract_deep_link_code`) стоит
    ПОСЛЕ проверки активных сессий упражнений (чтобы не перехватить
    случайный ответ внутри упражнения) и ДО перехвата Review/экрана
    приветствия — иначе код потеряется в комментарии психологу или в
    «Привет, {имя}!». Формат — ровно 6 цифр, у ответов упражнений такого
    формата не бывает, коллизий нет.
  - **Важный нюанс для совсем новых людей**: если это самое первое
    сообщение человека боту и код НЕ подошёл (скорее всего — случайное
    совпадение, а не попытка привязки), бот не пугает незнакомца «код
    устарел», а просто показывает обычное приветствие — как будто
    распознавания не было вовсе. Честную ошибку получает только уже
    знакомый человек, который откровенно пытается что-то привязать.
  - `vk_bot/config.py` — новая настройка `TELEGRAM_BOT_USERNAME` (публичная,
    не секрет). `vk_bot/handlers.py` — общий `_confirm_link_code_and_reply`
    убрал дублирование между ручным вводом кода и автораспознаванием.
  - **Тесты**: +12 в `tests/test_handlers.py` (разбор диплинка, обе
    подсказки-ссылки в зависимости от платформы генерации, автоподтверждение
    для знакомого и для совершенно нового человека, честный откат к
    приветствию при неверном коде у нового контакта, защита от перехвата
    ответа внутри активного упражнения). Полный пакет: 316/316.
  - **Деплой** — обычный `git pull` + `systemctl restart` всех трёх
    сервисов, миграций нет. Чтобы заработала ссылка из VK в Telegram,
    нужно один раз дописать в `.env` на сервере строку
    `TELEGRAM_BOT_USERNAME=<username бота без @>` и перезапустить ботов —
    без этого шага всё остальное (включая ссылку из Telegram в VK и
    обычный ручной ввод кода) работает как обычно.

- **03.09.2026 (Осознанный выбор — объединены экраны шага 4)** — на шаге
  «Анализ выбора» (`vk_bot/exercises/conscious_choice.py`) раньше сначала
  показывалось «Я выбираю: …» с просьбой нажать «Продолжить», и только
  вторым отдельным сообщением — «Не хочу» с примерами минусов; кнопка между
  ними ничего не спрашивала, просто разделяла два экрана. По просьбе
  пользователя оба текста слиты в одно сообщение, минусы можно вписать
  сразу. Заодно убрано уточнение «(опасные минусы)», добавлена пометка
  «Каждый раз разные примеры». Внутренняя нумерация шагов (4/5) сохранена
  ради арифметики «Назад»/«В начало»/«В конец» — оба номера теперь ведут на
  один и тот же объединённый экран. 8 тестов в `tests/test_exercises.py`
  обновлены под новое поведение, 139/139 в файле, 287/287 по всему пакету
  (без учёта тестов привязки аккаунтов ниже). Деплой — обычная правка кода
  без миграций, `git pull` + `systemctl restart`.

- **03.09.2026 (Привязка аккаунтов VK + Telegram)** — один и тот же человек
  теперь может объединить свой VK- и Telegram-аккаунт в один: прогресс,
  напоминания и проверки становятся общими, из какого мессенджера ни зайди.
  Решает пункт из `platform_bots/README.md` («Модель пользователя»), который
  раньше был отложен на будущее.
  - **Как это работает для пользователя** (по запросу — «максимально
    удобно, просто и понятно»): в главном меню кнопка «🔗 Привязать
    аккаунт» → «🔑 Получить код» показывает 6-значный код на 10 минут →
    этот код пишется обычным сообщением в ДРУГОМ мессенджере через «✍️
    Ввести код». Код можно вписать как угодно (с пробелами, дефисами,
    лишними словами) — бот вычленяет цифры сам; если цифр вообще нет,
    переспрашивает, а не падает и не списывает как использованный.
  - **Модель**: `bot_api.AccountLinkCode` (миграция `0010`) — код НЕ
    уникален глобально в БД (проверяется уникальность только среди
    активных/непросроченных кодов при генерации — не нужно на 6-значном
    пространстве держать уникальность вечно), живёт 10 минут,
    одноразовый (`used_at`).
  - **API**: `POST /link/generate/` и `POST /link/confirm/`
    (`bot_api/views.py::AccountLinkViewSet`) — честные коды ошибок вместо
    молчаливого сбоя: `already_linked` (оба id уже заполнены),
    `invalid_or_expired`, `same_account` (код из своего же аккаунта),
    `conflict` (оба аккаунта уже заняты РАЗНЫМИ id одной платформы — слить
    нельзя, не потеряв данные).
  - **Слияние `_merge_users()`**: аккаунт, СГЕНЕРИРОВАВШИЙ код — тот, что
    остаётся; аккаунт, ВВЕДШИЙ код — сливается и удаляется. Результаты и
    отзывы — переносятся простым bulk-update. Прогресс по упражнению
    (`unique_together=['user','exercise_type']`) — при конфликте оставляется
    более свежий по `updated_at`, второй удаляется. Напоминания —
    дедуплицируются по точному совпадению (иначе на два мессенджера
    задвоилось бы одно и то же напоминание). `streak` — берётся максимум,
    `last_activity_date` — более поздняя дата. Важный порядок операций:
    `target_user.delete()` — ДО `source_user.save()` с скопированными
    `vk_id`/`telegram_id` (оба поля уникальны в БД — иначе `IntegrityError`,
    пока старая запись ещё жива).
  - **Тесты**: 11 новых в `tests/test_bot_api_integration.py` (реальные
    Django-миграции + DRF + SQLite in-memory — включая оба варианта
    конфликта прогресса, дедупликацию напоминаний, повторное использование
    кода), 7 в `tests/test_api_client.py`, 17 в `tests/test_handlers.py`
    (меню, обе ошибки генерации, все 4 ошибки подтверждения, мусорный ввод
    кода, и что открытая проверка психолога не блокирует привязку — тот же
    паттерн смягчения, что раньше для «Напоминаний»/«Отправить на
    проверку»). Полный пакет: 304/304, регрессий нет.
  - **Деплой — единственный за это время требует миграции**: помимо
    `git pull` + `systemctl restart` (все три сервиса — `observatory`,
    `observer_bot`, `observer_bot_telegram`, поскольку `bot_api` меняется
    и веб, и оба бота), на сервере ОБЯЗАТЕЛЬНО `python manage.py migrate`
    до перезапуска ботов — иначе они полезут в несуществующую таблицу
    `bot_api_accountlinkcode`.

- **03.09.2026 (MAX и Одноклассники — черновики адаптеров сверены с реальной докой)** —
  `platform_bots/max_adapter.py` и `ok_adapter.py` раньше были честными
  "правдоподобными скелетами" (сам факт был явно написан в докстринге).
  Теперь оба сверены с официальной документацией по-настоящему (веб-поиск
  + чтение конкретных страниц доки, не по памяти):
  - **MAX** (dev.max.ru/docs-api): базовый URL `https://platform-api2.max.ru`,
    авторизация — заголовок `Authorization: <token>` (не query-параметр,
    как было в черновике), отправка — `POST /messages?user_id={id}` с
    телом `{"text": ...}`, приём — `GET /updates` (long polling,
    marker/limit/timeout) или `POST /subscriptions` (webhook). Не
    подтверждено докой — точные поля кнопки клавиатуры и точная форма
    апдейта `message_created` (сама схема Update и объекта Message
    подтверждены по отдельности, а не их совмещение) — помечено TODO.
  - **OK** (apiok.ru/dev/graph_api): официальный Bot API для сообщений
    РЕАЛЬНО существует, построен на Graph API (не классический REST OK
    со старой MD5-подписью — подпись тут не нужна вообще, только
    `access_token`). Базовый URL `https://api.ok.ru/graph`, запросы вида
    `/graph/{alias}/{edge}[/{subresource}]` (Facebook-Graph-подобная
    схема) — подтверждено примером `POST .../me/messages/chat:{id}` и
    `POST .../me/subscribe`. Не подтверждено докой — точная адресация
    1:1-сообщения пользователю (в доке нашлись два непримирённых
    варианта, взят по аналогии с chat:), поля кнопки клавиатуры, точный
    путь `GET .../updates` (страница без текстового примера) — везде
    TODO.
  - `platform_bots/base_adapter.py` заодно приведён в соответствие с
    РЕАЛЬНЫМ нейтральным форматом клавиатуры (`{"rows": [(текст, цвет),
    ...], "one_time": bool}`, как в `vk_bot/keyboards.py`) — раньше там
    был устаревший плоский пример из самого первого черновика (до шага 2
    плана миграции), не соответствующий тому, чем реально пользуются
    `VkAdapter`/`TelegramAdapter`.
  - Ничего из этого не подключено к живому боту (как и раньше) — можно
    смело деплоить, риска для VK/Telegram-бота нет. 269/269 тестов не
    затронуты (`platform_bots/` тестами не покрыт).

- **03.09.2026 (Права форума выданы)** — раздела форума и достижения на
  проде оказались уже фактически рабочими (миграция `0007` и все миграции
  `machina_*` были применены раньше, разделы форума заполнены через
  `seed_forum_topics.py`), но самих прав чтения/ответа для обычных
  пользователей не было — они не назначаются автоматически при создании
  `Forum` напрямую через ORM (сигнал в `django-machina` срабатывает только
  при создании форума через её собственный admin-flow, а он тут не
  использовался). Плюс важная деталь инфраструктуры: у проекта СВОЙ
  `CustomAdminSite` (`config/admin.py`), в него явно зарегистрированы
  только модели `bot_api`/`myapp`/`User`/`Group` — django-machina
  регистрирует свои модели (включая права) в отдельную, никуда не
  подключённую стандартную админку Django, поэтому раздела «💬 Форум» в
  `/admin/` нет и не будет, пока её не зарегистрировать в `CustomAdminSite`
  отдельно (не сделано, не критично). Права выданы напрямую через ORM
  (`python manage.py shell`, `UserForumPermission.objects.update_or_create(...,
  anonymous_user=False, authenticated_user=True, ...)` — официальный
  механизм `machina`, применяется сразу ко всем аутентифицированным
  пользователям без привязки к Django-группе) для всех 5 текущих разделов
  (`can_see_forum`, `can_read_forum`, `can_start_new_topics`,
  `can_reply_to_topics`, `can_edit_own_posts`, `can_delete_own_posts`).
  Выполнено на проде, без ошибок.

- **03.09.2026 (Telegram-бот, шаг 5/5 — фоновые напоминания и комментарии психолога)** —
  последний оставшийся кусок плана `platform_bots/README.md`: теперь
  `NotificationSystem` (`vk_bot/notifications.py`) умеет слать и через
  Telegram, не только через VK. Что сделано:
  - `NotificationSystem.__init__(vk_session, api_client, platform='vk')` —
    новый параметр `platform`, по умолчанию `'vk'`, поведение для VK не
    изменилось ни на бит (все существующие тесты `test_notifications.py`
    прошли без единой правки). Для `platform='telegram'` отправка идёт через
    `TelegramAdapter.send_text()` (новый метод, НЕ глотает исключения — в
    отличие от `send_message`, который используется в диалоге упражнения) —
    `NotificationSystem` сам ловит `requests.exceptions.HTTPError` и по
    `response.status_code == 429` определяет флуд-контроль Telegram (аналог
    кодов 6/9 у VK `ApiError`) и останавливает рассылку в этом цикле, как и
    для VK.
  - **Общий список due-уведомлений/комментариев на два бота**: и
    `/notifications/due/`, и `/admin/review/pending_admin_comments/` не
    фильтруют записи по платформе — оба процесса (VK и Telegram, каждый
    в своём systemd-сервисе) видят один и тот же список целиком.
    `NotificationSerializer` и `pending_admin_comments` теперь отдают ОБА
    поля — `user_vk_id` и `user_telegram_id` (одно из них `null`, зависит от
    платформы пользователя) — `NotificationSystem._extract_platform_user_id()`
    по ним отличает «запись для другой платформы» (молча пропустить, это
    нормально) от «битой записи без ID вообще» (залогировать warning, как
    и раньше).
  - `vk_bot/handlers.py`: `NotificationSystem(vk_session, self.api,
    platform=api_platform)` — раньше платформа не передавалась вовсе.
  - `vk_bot/main_telegram.py`: `start_notifications=True` (было `False`) —
    фоновый поток напоминаний теперь поднимается и для Telegram-процесса.
  - Тесты: 9 новых в `test_notifications.py` (Telegram-ветка — due-напоминание,
    комментарий психолога, флуд-контроль 429, "чужая" запись пропускается
    молча, битая запись без ID варнится), ни один старый тест не менялся.
    Всего 269/269 в полном наборе (было 260).
  - **Подтверждено на проде** (03.09.2026, ~10:01 UTC): `git pull` +
    рестарт всех трёх сервисов (`observatory`, `observer_bot_telegram`,
    `observer_bot`) прошли чисто. В логах `observer_bot_telegram` впервые
    появилась строка `notifications - INFO - Notification system started`
    (при предыдущих рестартах её не было — фоновый поток раньше не
    поднимался для Telegram) — значит, `start_notifications=True` реально
    заработал. `observer_bot` (VK) и `observatory` (Django) тоже подняты
    без ошибок. Периодические `getUpdates error`/`ConnectTimeoutError` в
    логах Telegram-бота — старая, уже опознанная сетевая особенность
    VPS → api.telegram.org (не связана с этой правкой, ретрай справляется
    сам, см. «Известные грабли»).

- **03.09.2026 (Telegram-бот, шаг 4/4 — main_telegram.py, упражнения работают)** —
  финальный шаг плана `platform_bots/README.md`. Что сделано:
  - **`bot_api.User`**: добавлено поле `telegram_id` (миграция
    `0009_user_telegram_id_alter_user_vk_id`), `vk_id` стал необязательным
    (`null=True`) — теперь запись пользователя представляет либо VK-, либо
    Telegram-аккаунт. Один и тот же числовой ID у VK и Telegram — это ДВЕ
    разные записи (проверено тестом
    `test_vk_and_telegram_users_with_overlapping_numeric_ids_do_not_collide`).
  - **`bot_api/views.py`**: все эндпоинты, что ищут пользователя (`/users/`,
    `/progress/*`, `/results/`, `/admin/review/*` кроме `pending_admin_comments`
    и `mark_comment_sent`, `/notifications/` кроме `due`/`mark_sent`) теперь
    принимают `telegram_id`/`user_telegram_id` наравне с `vk_id`/`user_vk_id`
    через общий хелпер `_platform_id_kwargs()`. Существующие VK-запросы не
    меняются ни на бит — телеграм-параметры просто добавились рядом.
  - **`vk_bot/api_client.py`**: `APIClient(platform='vk'|'telegram')` — сам
    решает, какое имя поля слать (`_id_field()`), остальной код не меняется.
  - **`vk_bot/exercises/base.py` / `stress_search.py`**: `self.platform`
    теперь умнее — если `vk_session` уже сам реализует `send_message`
    (например `TelegramAdapter`), используется напрямую, без обёртки в
    `VkAdapter` (раньше, на шаге 3, оборачивался БЕЗУСЛОВНО — это был бы
    скрытый баг: `VkAdapter.send_message` зовёт `vk.method(...)`, которого
    у `TelegramAdapter` нет, и первое же сообщение из упражнения упало бы).
    Нашли и поймали живым end-to-end прогоном через `TelegramAdapter`
    ДО деплоя, не тестами — стоит иметь в виду на будущее.
  - **`vk_bot/handlers.py`**: `BotHandlers(vk_session, api_platform='vk',
    start_notifications=True)` — та же hasattr-развилка для `self.platform`,
    `APIClient(platform=api_platform)`, и `start_notifications=False`
    отключает фоновый поток `NotificationSystem` (см. ниже, почему).
    Вызов из `vk_bot/main.py` не меняется (`BotHandlers(vk_session)` —
    все новые параметры со значениями по умолчанию).
  - **`platform_bots/telegram_adapter.py`**: `run()` теперь передаёт в
    `on_message` ещё `first_name`/`last_name` (Telegram отдаёт их прямо в
    апдейте, `message['from']` — в отличие от VK, отдельный запрос вроде
    `users.get` не нужен). `_to_reply_markup()` переведён с чернового
    плоского формата на реальный нейтральный формат клавиатур из шага 2
    (`{"rows": [[(текст, цвет), ...]], "one_time": bool}`) — до этой правки
    он был рассинхронизирован с `keyboards.py` и сломал бы любую отправку
    с кнопками.
  - **`vk_bot/main_telegram.py`** (новый) — точка входа для Telegram, по
    образцу `main.py`: `TelegramAdapter` + `BotHandlers(adapter,
    api_platform='telegram', start_notifications=False)`, свой
    long-polling цикл с ретраем. Отдельный процесс, отдельный systemd-сервис
    (`observer_bot_telegram`, ещё предстоит завести на сервере) — `main.py`
    и `observer_bot` не трогает и не перезапускает.
  - **Осознанно отложено на потом**: фоновые напоминания и проактивная
    доставка комментариев психолога (`notifications.py::NotificationSystem.
    _check_loop`) умеют слать только через VK (`self.vk.method` напрямую).
    Настройка напоминания через меню «Напоминания» в Telegram всё равно
    работает и сохраняется в БД (это чистая запись через `api_client`,
    платформонезависимая) — просто пока никто её не доставит вовремя, пока
    для Telegram не заведён свой запуск этого фонового цикла. Это
    сознательное решение (обсуждали с пользователем), не забытый баг.
  - Тесты: 260/260 зелёных (256 старых + 4 новых на `telegram_id` в
    `tests/test_bot_api_integration.py`), плюс отдельный сквозной прогон
    (не тест, ручная проверка) — `BotHandlers` с настоящим `TelegramAdapter`
    прошла весь путь главное меню → упражнения → «Поиск стресса» → часть 1
    без единой ошибки, включая честную обработку сбоя сети.
  - **Что нужно сделать на сервере** (не делает эта сессия — нет доступа):
    `TELEGRAM_BOT_TOKEN` в `.env` (см. `.env.example`), `python manage.py
    migrate` (миграция 0009), новый systemd unit `observer_bot_telegram`
    (`ExecStart=... python vk_bot/main_telegram.py`, по образцу
    `observer_bot`, но НЕ через тот же unit).
  - **Подтверждено вживую на проде (03.09.2026)** — после деплоя поймали и
    починили 3 живых проблемы, которых не было в тестах:
    1. `bot_api_user` в Postgres принадлежал старой роли, не
       `observatory_user` (сервис-аккаунт с 01.09.2026) — `ADD COLUMN`
       падал с `must be owner of table`. Починено: `ALTER TABLE/SEQUENCE
       ... OWNER TO observatory_user` для всех `bot_api_*` разом (не
       только `bot_api_user` — чтобы не наступить на то же с другой
       таблицей на следующей миграции).
    2. `ImportError: cannot import name 'TELEGRAM_BOT_TOKEN' from
       'config'` — в проекте ДВА модуля `config`: `vk_bot/config.py` и
       Django-пакет `config/` в корне (см. предупреждение в самом начале
       `tests/test_bot_api_integration.py` про этот же конфликт имён).
       `main_telegram.py` изначально клал корень репозитория в sys.path
       через `insert(0, ...)` — это ставило `config/` (Django) ПЕРЕД
       `vk_bot/`, и импортировался пустой пакет вместо
       `vk_bot/config.py`. Починено: `sys.path.append(...)` вместо
       `insert(0, ...)` — `vk_bot/` (её кладёт первой сам Python как папку
       запускаемого скрипта) остаётся в приоритете.
    3. Токен в `.env` дважды вставился неправильно (плейсхолдер из
       примера команды, потом — с угловыми скобками `<...>` вокруг) —
       человеческая ошибка при копировании, не баг кода.
    После починки — реальный диалог в Telegram подтверждён: бот
    поздоровался с пользователем по имени (`get_or_create_user` подтянул
    `first_name` из Telegram-апдейта верно) и показал главное меню с той
    же клавиатурой, что и VK-версия.

- **03.09.2026 (Telegram-бот, шаг 3/4 — self.platform в упражнениях)** —
  продолжение шага 2. Добавлен `vk_bot/vk_adapter.py` — класс `VkAdapter`
  (по форме совпадает с `platform_bots/base_adapter.py::MessagingAdapter`,
  но не импортирует его напрямую: `vk_bot/main.py` запускается отдельным
  скриптом, и в проде в sys.path попадает только папка `vk_bot/`, а не
  корень репозитория — прямой межпапочный импорт рискованно ломать без
  необходимости). `vk_bot/exercises/base.py::BaseExercise` и
  `vk_bot/exercises/stress_search.py::StressSearchExercise` теперь держат
  `self.platform = VkAdapter(lambda: self.vk)` и их `send_message` зовёт
  `self.platform.send_message(...)` вместо прямого `self.vk.method(...)`.
  Важный нюанс, из-за которого адаптер принимает функцию-геттер, а не
  сам vk_session: тесты (`tests/test_exercises.py`) в нескольких местах
  подменяют `экземпляр.vk` УЖЕ ПОСЛЕ создания упражнения (переиспользуют
  тот же fake-vk между экземплярами при restart/resume) — если бы
  `VkAdapter` захватывал `vk_session` один раз в конструкторе, такая
  подмена перестала бы работать и сообщения молча уходили бы «не туда».
  `self.vk` оставлен как есть (не удалён) — именно на него ссылается
  геттер, и другие 5 упражнений (`happiness_list`, `my_roles`,
  `conscious_choice`, `diary`, `stop_technique`) наследуют `BaseExercise`
  и получили `self.platform` бесплатно, без своих правок. Поведение не
  изменилось: все 256 тестов зелёные без единой правки в них самих.
  Остался последний шаг плана — `main_telegram.py` отдельным
  systemd-сервисом (`observer_bot_telegram`), не трогающим VK-бот.

- **03.09.2026 (Telegram-бот, шаг 2/4 — нейтральные клавиатуры)** — начали
  подключение Telegram-бота к упражнениям по плану из `platform_bots/README.md`.
  Шаг 1 (проверка `telegram_adapter.py` живым эхо-ботом через @BotFather)
  пройден. Шаг 2: `vk_bot/keyboards.py` переписан — теперь каждая из 18
  функций (`main_menu`, `exercise_keyboard` и т.д.) возвращает не готовый
  VK-объект, а нейтральную структуру `{"rows": [[(текст, цвет), ...], ...],
  "one_time": bool}` (цвет — платформонезависимая строка
  `positive/negative/primary/secondary`). Готовый VK JSON теперь собирает
  отдельная функция `to_vk_keyboard()` в том же файле — она вызывается ровно
  в 3 местах, где сообщение реально уходит в VK: `vk_bot/exercises/base.py`
  (`BaseExercise.send_message` — общий для 5 из 6 упражнений),
  `vk_bot/exercises/stress_search.py` (свой `send_message`, класс не
  наследует `BaseExercise`) и `vk_bot/handlers.py`
  (`BotHandlers.send_message`). Текстовые константы (`CONTINUE_TEXTS` и
  т.д.) не трогали — они и так платформонезависимы. Поведение VK-бота не
  изменилось ни на бит: все 256 тестов зелёные без единой правки в самих
  тестах, вручную сверили — цвета кнопок, разбивка на ряды и `one_time`
  в итоговом VK JSON совпадают с тем, что было раньше. Дальше по плану —
  шаг 3 (завести `self.platform`/`MessagingAdapter` в `BaseExercise` и
  `StressSearchExercise`), потом шаг 4 (`main_telegram.py` отдельным
  systemd-сервисом, VK-бот не трогает).

- **02.09.2026 (бэкап на Яндекс.Диск)** — `scripts/backup_db.sh` (нашли,
  что бэкапы БД хоть и делались каждую ночь по крону, но лежали только на
  самом сервере — при потере сервера целиком пропали бы вместе с ним).
  Теперь после локального дампа скрипт дополнительно копирует свежий
  `.sql.gz` (и, если появится, `media_*.tar.gz`) на Яндекс.Диск через
  `rclone` (WebDAV, `webdav.yandex.ru`, remote с именем `yandex` в
  `rclone.conf`, папка на Диске — `Observatory-backups`). Если `rclone` не
  установлен или remote не настроен — шаг молча пропускается, остальной
  бэкап всё равно отрабатывает как раньше.
  Живые грабли при настройке (на будущее, если придётся переносить на
  другой сервер): (1) `user` для WebDAV — полный email
  (`login@yandex.ru`), короткий логин даёт 401; (2) без `--bind 0.0.0.0`
  соединение иногда уходило по IPv6 и заливка файла зависала намертво
  (мелкие запросы вроде списка папок по IPv6 проходили нормально — ловится
  только на реальной передаче файла); (3) у Яндекс.Диска по WebDAV
  заметно урезана скорость (~30-60 КБ/с) — стандартного таймаута мало,
  поставили `--timeout 180s`; (4) для одного файла нужен
  `rclone deletefile`, не `rclone delete` (последний трактует путь как
  папку и падает с «directory not found» на одиночном файле).
  Пароль приложения — отдельный, для WebDAV
  (https://id.yandex.ru/security/app-passwords), НЕ обычный пароль от
  аккаунта.
- **02.09.2026 (глубокая проверка кода)** — по запросу «проверь весь код
  максимально глубоко на баги»: 5 параллельных агентов прошлись по всему
  проекту (упражнения бота, ядро vk_bot, myapp, config/bot_api/crm/
  observer3d/platform_bots, шаблоны), каждую находку перепроверил лично
  (включая один ложный вызов — `terms_of_service.html` показался
  отсутствующим только из-за неполной локальной копии, на сервере файл
  есть). Исправлено 7 подтверждённых багов:
  - **Потеря данных (самый серьёзный)** — в «Моих ролях» и «Поиске
    стресса» кнопка «Сохранить и начать заново» при сбое сохранения на
    сервере (сеть/сервер недоступны) всё равно стирала прогресс и
    открывала пустую сессию, хотя пользователю говорилось «ничего не
    потеряно» — во всех ОСТАЛЬНЫХ 4 упражнениях эта дыра уже была закрыта
    раньше, здесь пропустили. Добавлена та же защита (`if not
    self._finish(...): return`), плюс 2 новых теста-регрессии.
  - Кнопка «📅 Мой план на день» в разделе с открытой проверкой у
    психолога срабатывала неверно — распознавалась не как переход в меню,
    а как ответ психологу (сравнение шло по слову «план», а реальный текст
    кнопки после нормализации — «мой план на день»).
  - `get_user_results` при сетевом сбое возвращал `[]` — неотличимо от
    «упражнений правда нет», из-за чего «Мой путь»/«Вся история»/«Проверка»/
    «План на день» при временном сбое сервера показывали пользователю
    пустую историю вместо честного «сервис недоступен» (тот же паттерн, что
    раньше уже поправили для напоминаний).
  - Ежедневные напоминания в боте срабатывали на 3 часа позже настроенного
    времени — сравнение времени в `bot_api/views.py` шло в UTC вместо
    московского (`timezone.now()` без `localtime()`).
  - Открытый редирект на экране ввода кода 2FA (`config/twofa_views.py`) —
    параметр `?next=` не проверялся, добавлена та же проверка
    `url_has_allowed_host_and_scheme`, что уже стоит на входе через VK ID.
  - Гонка при создании нового типа упражнения (`bot_api/views.py`,
    `ResultViewSet.create`) — при первом результате нового упражнения от
    двух пользователей одновременно могла завестись задвоенная запись
    `Exercise`; заменено на `get_or_create`.
  - Дубли публикации поста в канал — cron (раз в 5 мин), вебхук-будильник
    и ручная кнопка «Опубликовать сейчас» в CRM могли одновременно
    подхватить одну и ту же запись `PostChannelStatus` со статусом
    'scheduled'/'failed' и опубликовать её дважды; логика вынесена в общую
    `crm/publish_logic.py` с блокировкой строки (`select_for_update`
    внутри транзакции) и перепроверкой статуса перед публикацией.
  256 тестов зелёные (было 253 + 3 новых на баг с потерей данных).
  Отдельно, не бага ради — миграция
  `myapp/migrations/0007_achievement_moduletestresult_userachievement.py`
  существует только на сервере (сделана там же через `makemigrations`) и
  никогда не попадала в git и на этот компьютер — стоит скачать её с
  сервера и закоммитить, иначе она нигде не сохранена как резервная копия.
- **02.09.2026 (ночь, 6)** — продолжение блока «форум/достижения» (см. ниже):
  - Новая страница `/statistics/` (сравнение «стресс → счастье» по данным
    упражнений бота: сколько образов отпущено/ещё пугают, средняя оценка до
    и после разбора, топ-3 из списка счастья). Данные из `bot_api.Result`,
    связь с пользователем сайта — по той же схеме имени `vk_{vk_id}`, что
    уже использует CRM. Ссылка в шапке сайта.
  - Ещё 2 достижения: «Собиратель» (первая закладка) и «Открытая книга»
    (профиль заполнен полностью — о себе, город, сайт).
  - Страница `/faq/` (Вопросы и ответы) — ссылка в подвале сайта.
  - Страница «Тропы» (`/put/`) была заглушкой «В разработке» — заполнена
    атмосферным текстом в теме тумана/фонарика и карточками по каждому
    упражнению бота, плюс ссылки на курс/карту/форум/достижения/статистику.
  - Черновик `seed_forum_topics` — создаёт по одной приветственной теме в
    каждом разделе форума (Topic+Post через django-machina). Как и
    `seed_forum`, не проверен вживую (нет machina в песочнице) — сначала
    `--dry-run` (по умолчанию), при ошибке показать её текст.
  Деплой — обычный (новых моделей/миграций в этом блоке нет).
- **02.09.2026 (ночь, 5)** — Django-сайт: включён форум (django-machina, был
  настроен, но не подключён в urls.py), новая система достижений/геймификации
  (10 достижений, страница `/achievements/`, узел на ментальной карте),
  результаты теста модуля теперь сохраняются на сервере (раньше только в
  браузере), плюс черновая архитектура ботов под другие платформы
  (`platform_bots/`: Telegram — рабочий черновик, MAX/OK — скелеты). Подробности
  и порядок деплоя (нужны миграции) — см. раздел «Django-сайт: форум,
  достижения...» выше. `vk_bot/` и его 253 теста не затронуты.
- **02.09.2026 (ночь, 4)** — «Осознанный выбор»: убраны кнопки
  Назад/В начало/В конец с клавиатуры Шага 1 (были лишними на этом экране);
  добавлена поддержка вставки списком на Шаге 1 (как в «Моих ролях» и
  «Списке счастья» — можно вставить сразу несколько пунктов, каждый со
  своей строки); Шаг 2 разбит на два экрана — сначала бот просит
  сформулировать СВОЮ фразу-утверждение («Я имею право не хотеть ...») по
  примеру с ДРУГИМ, нейтральным пунктом (специально не похожим на
  написанное пользователем — иначе фразу можно было бы просто
  скопировать, не подумав), и только потом показывает вопрос «Кто отнял у
  тебя это право»; переписан текст Шага 3 («Кто круче?», убрана
  промежуточная строка «Теперь ответь на вопрос»). 253 теста зелёные,
  задеплоено и перезапущено на сервере.
- **02.09.2026 (ночь, 3)** — «Поиск стресса»: адверсариальное ревью (свежий
  агент, не видевший работу) нашло 3 непритичных места, все починены:
  1. Черновик экрана подтверждения Вопроса 1 (написанные, но ещё не
     подтверждённые «➡️ Продолжить» варианты «как должно быть») не входил
     в `save_progress` — при обрыве процесса именно на этом экране варианты
     терялись молча. Теперь черновик сохраняется отдельным полем
     (`confirm_ideal_draft`) и восстанавливается — resume честно показывает
     тот же экран с теми же вариантами, а не чистый Вопрос 1/4.
  2. Пауза «между образами» (после поздравления с новой оценкой) тоже не
     персистилась — при обрыве именно на ней resume перескакивал сразу к
     следующему образу, пропуская экран с кнопками. Теперь флаг
     `_between_items` тоже сохраняется/восстанавливается.
  3. Мелкая текстовая нестыковка: resume-показ Вопроса 2/4 (один вариант)
     не включал пояснение «Это твой прогноз...» — добавлено, текст
     идентичен обычному показу.
  Заодно текст Вопроса 3/4 («· Объясни») заменён на конкретную подсказку
  «Представь эту ситуацию, когда будет 100%, и опиши её так, как ты это
  видишь» — по просьбе пользователя, во всех трёх местах (одиночный
  вариант, разбор по вариантам, resume). +3 теста на устойчивость к
  обрыву сессии, весь набор зелёный (246 passed).
- **02.09.2026 (ночь, 2)** — «Поиск стресса»: если на экране подтверждения
  Вопроса 1 записано НЕСКОЛЬКО вариантов «как должно быть» (переписывал
  несколько раз), Вопрос 2/4 (процент реальности) и Вопрос 3/4 (почему)
  теперь спрашиваются ОТДЕЛЬНО по каждому варианту, а не одним общим
  числом/объяснением на все сразу — с индикатором «вариант N/M». Общий
  `percent` в ответе — среднее по вариантам (для статистики реалистичности),
  а полный разбор по каждому — в новом поле `ideal_details` (список
  {text, percent, why}), которое доходит и до финального связного разбора
  перед новой оценкой, и до result_data (сохраняется через save_result —
  значит видно и в статистике на сайте). Один вариант — поведение как
  раньше, без изменений. +2 теста взамен устаревшего, весь набор зелёный
  (243 passed).
- **02.09.2026 (ночь)** — «Поиск стресса»: на экране Вопроса 1/4 добавлена
  кнопка «✏️ Изменить пункт» — если противоположность не находится, потому
  что сам пункт неточно сформулирован, можно тут же поправить его текст и
  оценку (формат «Текст 9»), не выходя из разбора. Правка проверяется на
  дубли с другими пунктами карты (сама с собой не считается) и обновляется
  одновременно в трёх местах: в списке образов, в текущем разбираемом
  пункте и в уже создан­ной записи ответа — так что изменение доходит и до
  итоговой сводки в конце упражнения, и до result_data, которое сохраняет
  save_result (это тот же JSON, что читает статистика на сайте — отдельных
  правок на сайте не потребовалось). +5 новых тестов, весь набор зелёный
  (241 passed). Деплой: файлы переданы пользователю, дальше — git push +
  git pull + restart на сервере (см. команды ниже).
- **02.09.2026 (вечер)** — «Поиск стресса»: атмосферный редизайн под тему
  «туман/фонарик/дорога» (по запросам пользователя, серия мелких правок за
  один вечер), 233 теста зелёные, задеплоено и перезапущено на сервере:
  - Новые тексты: интро («🎯 ОХОТА НА СТРЕСС»), меню упражнений («🌫️ ДОРОГА
    К СЕБЕ»), формула стресса («напряжение» вместо «разрыв»), фраза про
    прогноз карты.
  - Постоянный счётчик «🟢 N уже отпущено · 🔴 M ещё пугают» по всей записанной
    карте (оценка 0-3 = отпущено, выше — ещё пугает), виден во всех
    сообщениях части 1.
  - Условие досрочного завершения («✅ Завершить и отправить») теперь всегда
    показывает, сколько ещё нужно записать/разобрать, а не молчит, пока
    порог не достигнут; кнопку перенесли в самый низ клавиатуры.
  - Итоговое сообщение («Путь завершён») сделано компактнее (убран заголовок,
    повтор формулы), не показывает больше ложные «0% реалистичности», когда
    разбора не было, и честно указывает, что отправка наблюдателю — отдельный
    шаг через «📨 Проверка» в меню (завершение само по себе не отправляет).
  - Прогресс-бар переделан под тему (🔦 фонарик + ▮ пройдено + · туман).
  - Вопрос 1/4 (противоположность): убраны кнопки «Да»/«Нет», оставлена
    одна «➡️ Продолжить»; экран всегда показывает последний написанный
    вариант (не список всех попыток) — раньше при передумывании подряд
    несколько раз бот просто повторял «нажми кнопку», не показывая, что
    человек вообще написал.
  - Вопрос 3/4: подставляется реальный введённый процент («Увеличиваем с
    твоих N% до 100%») вместо статичной фразы.
  - Вставка списком образов: показывает первые 10 (не 30), остальное не
    теряется («уже на карте, ниже общий счёт»), просто не расписывается
    построчно.
- **02.09.2026** — Глубокая проверка ВСЕГО бота (два независимых агента-ревьюера
  прошлись по handlers.py/notifications.py/api_client.py/config.py/main.py и
  всем 6 упражнениям) — исправлено абсолютно всё найденное, 233 теста зелёные:
  - **handlers.py**: 4 ветки «Напоминания» (1 час/3 часа/завтра утром/отключить)
    и отправка на проверку («Проверка» → номер упражнения) раньше подтверждали
    успех, не глядя на ответ API — теперь честно сообщают о сбое и не молчат о
    нём. Починен перехват сообщений при активной проверке психолога: слово
    «план» не было в белом списке, а разделы «Напоминания»/«Отправить на
    проверку» были фактически недоступны, пока висит открытая проверка — любое
    нажатие внутри них улетало психологу как комментарий вместо того, чтобы
    отработать.
  - **notifications.py**: убраны дубли отправки одного уведомления дважды за
    цикл, обрыв всего цикла из-за одной битой записи, добавлена обрезка
    длинных комментариев психолога (лимит VK ~4096 симв.) и повторные попытки
    (retry) для mark_sent при транзиентном сбое сети.
  - **api_client.py**: `get_or_create_user` раньше возвращал то список, то
    dict в зависимости от того, найден пользователь или создан — теперь всегда
    единый dict. `get_notifications` раньше маскировал сетевой сбой под «нет
    напоминаний» ([]) — теперь чётко отличает сбой (None) от честной пустоты.
    Убран мёртвый код: `get_user_stats` (дёргал несуществующий эндпоинт),
    `complete_review`/`get_review_status`/`get_review_status_by_type` (ни разу
    не вызывались — реальное закрытие проверки происходит в CRM напрямую).
  - **my_roles.py**: защита от IndexError в разборе ролей; если суточный лимит
    на разбор истекал ровно в момент, когда пользователь писал следующее
    сообщение, оно раньше проглатывалось как «ответ» на вопрос про роль,
    которую ему ни разу не показали — теперь роль показывается заново;
    «Продолжить»/«Завершить», написанные как обычный текст во время разбора
    роли, раньше молча терялись (бот не отвечал вообще ничего).
  - **stop_technique.py / diary.py / happiness_list.py**: «Сохранить и начать
    заново» на пустой сессии (ни одного ответа) раньше создавало на сервере
    пустой результат — теперь просто перезапускает без сохранения. Если
    save_result падал на сервере, бот честно предупреждал «ничего не
    потеряно», но следующей же строкой удалял этот самый черновик и открывал
    пустую сессию — ответы терялись насовсем. Теперь при сбое сессия
    сохраняется как есть, и ту же кнопку можно нажать повторно.
  - **conscious_choice.py** — редизайн (по образцу «Поиска стресса»): глубокий
    разбор (шаги 2-9: «имею право не хотеть», «кто круче Бога», минусы/плюсы
    выбора и альтернатив) раньше проходил только для ПОСЛЕДНЕГО записанного
    пункта «Что я должен» — до 19 остальных пунктов сохранялись в списке, но
    полностью выпадали из итогового анализа. Теперь разбор идёт по очереди по
    КАЖДОМУ записанному пункту (с индикатором «Разбираем пункт N/M»), плюс тот
    же гейт на пустое сохранение и защита от потери прогресса при сбое.
  Деплой подтверждён: на сервере `git pull` + `python -m pytest tests/ -q`
  → 233 passed, `systemctl restart observer_bot` выполнен, сервис работает
  штатно.
- **01.09.2026 (2)** — «Поиск стресса»: независимая проверка (свежий агент,
  не видевший работу) нашла, что оба новых порога досрочной отправки можно
  было обойти через СТАРЫЕ кнопки: экран входа в разбор («➡️ Далее» / «✅
  Завершить», handle_analysis) и «💾 Сохранить и начать заново»
  (_handle_save_and_start_over) вызывали _finish_exercise() без всякой
  проверки — можно было отправить с 1 образом и 0 разобранных. Плюс
  session['answers'] мог включать незавершённую запись текущего образа
  (без 'new_rate') в счётчик «разобрано» и в отправляемый analysis. Правки:
  добавлен единый метод `_can_finish_early()` (используется теперь везде,
  где можно закончить упражнение) и `_completed_answers()` (считает только
  реально законченные разборы). Также поправлена формулировка в интро —
  было «получишь комментарий» (звучало как гарантия сразу), стало
  «появится возможность отправить» (комментарий психолог оставит, когда
  сам проверит). Плюс мелочи: клавиатура при resume части 1 не показывала
  кнопку «Завершить и отправить», даже если порог уже пройден; убран
  мёртвый код (недостижимая проверка CANCEL_TEXTS в handle_question).
  +6 новых тестов на закрытые дыры, весь набор зелёный (105 passed).
- **01.09.2026** — «Поиск стресса»: правка текста подсказки к вопросу 1;
  добавлена досрочная отправка на проверку двумя независимыми путями
  (10 записанных в части 1 / 3 разобранных в части 2), не дожидаясь конца
  упражнения. См. пункт 1 в «VK-бот — упражнения» выше. Тесты:
  tests/test_exercises.py (+9 новых), весь набор зелёный (100 passed).
  Задеплоено на сервер, observer_bot перезапущен.
