**Название задачи:** Последовательность умножений

**Условие:**

Напишите функцию `calculate_sequence(n)`, которая принимает одно целое число `n`. В начале возьмите число `n`. Умножьте его на его же первую цифру. Результат умножьте на первую цифру результата. И так далее. Начните с `n` и продолжайте этот процесс, пока первая цифра очередного числа в последовательности не станет равной 1 или пока число не превысит миллиарда. В качестве ответа верните результат.

**Описание функции:**

```python
def calculate_sequence(n):
    # ваш код здесь
```

**Пример использования:**

```python
print(calculate_sequence(8))
# Ожидаемый вывод: 1152

print(calculate_sequence(223))
# Ожидаемый вывод: 1784

print(calculate_sequence(929))
# Ожидаемый вывод: 1605312

print(calculate_sequence(360))
# Ожидаемый вывод: 1080
```

- Функция должна использовать цикл `while`.
